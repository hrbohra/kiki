# deps
node_modules/
.pnpm-store/

# builds
dist/
build/
.turbo/
*.tsbuildinfo

# env / secrets (never commit) — populate .env from .env.example
.env
.env.*
!.env.example

# generated club-sized world (regenerate with `pnpm --filter @kiki/api seed:world`)
apps/api/import/world.seed.json

# expo / rn
.expo/
web-build/
dist-web/

# misc
coverage/
.DS_Store
*.log

# local media uploads (dev storage)
.uploads/
