import { OnboardingWeb } from './web/OnboardingWeb';

/** First-run invite / onboarding flow. One responsive layout for both web and phone. */
export function OnboardScreen() {
  return <OnboardingWeb />;
}
