// @kiki/voice — the plug-in port + blueprint for turning Kiki's ~10k-conversation corpus into the
// LLM's voice. Voice/style ONLY; never content, matching, or ranking. See VOICE_PIPELINE.md.
export * from './types';
export * from './corpus-voice.provider';
export * from './pipeline';
